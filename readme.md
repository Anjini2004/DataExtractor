Data Extraction Using LLama and Groq

This is a simple example of using a language model (LLama) and Groq to extract structured data from PDF files. The agent can read PDF files, extract text, and return structured JSON data.
The main components are:


